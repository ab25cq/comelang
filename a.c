#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <errno.h>

struct sData {
    int fds_bits[128];
};

int main()
{
    int _fd;
    sData* _p;
    
    _p->fds_bits[(int)_fd / (sizeof(__int32_t) * 8)];
    
    return 0;
}
