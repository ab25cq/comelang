#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <errno.h>

int main()
{
    int token_seen, line_ref;

    for(;;) {
        if (!token_seen)
            continue;
        ++line_ref;
    }
    
    return 0;
}
